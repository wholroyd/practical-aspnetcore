# Memory Pool Eviction

This sample demonstrates automatic memory pool eviction in .NET 10 for long-running applications to reduce memory footprint.

Run with `dotnet watch` inside the sample directory.
