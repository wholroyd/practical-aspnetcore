# Form Validation - Nested Objects

This sample demonstrates built-in validation for nested objects and collections in Minimal APIs using `AddValidation()`.

Run with `dotnet watch` inside the sample directory.
