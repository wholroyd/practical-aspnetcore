# HTMX hx-trigger click once

This example shows `click` event trigger with modifier `once`([doc](https://htmx.org/docs/#trigger-modifiers)). 

```html
<div hx-get="/htmx" hx-trigger="click once"></div>
```

